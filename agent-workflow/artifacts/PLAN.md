<!-- Generated by next-codex-workflow-kit -->
# Task Summary

Fix the unwanted yellow circle that appears on the landing page and the Pokemon profile page.

The issue is a focused UI bug, not a visual-system redesign. The current codebase already has the likely source: duplicated amber radial decoration blocks in the two hero components that anchor those routes.

# Affected Area Summary

- `src/components/home/home-hero.tsx`
  - Renders the landing-page hero decoration, including an amber radial circle.
- `src/components/pokemon-detail/pokemon-detail-hero.tsx`
  - Renders the profile-page hero decoration, including a similar amber radial circle.
- `src/app/globals.css`
  - Shared visual system reference point only; inspect to ensure the issue is not coming from a shared primitive before making route-local fixes.

# Implementation Approach

Treat this as a minimal route-level decoration fix.

The recommended implementation is:

- remove or neutralize the amber radial circle decoration in the landing-page hero
- remove or neutralize the matching amber radial circle decoration in the Pokemon detail hero
- preserve the rest of the atmosphere:
  - keep the blue radial glow
  - keep structural borders and panel styling
  - do not touch shared accent variables unless the route-level fix fails

What stays out of scope:

- changing the overall color system
- redesigning the hero layouts
- altering shared `Panel` or global shell behavior
- touching other route surfaces unless the same artifact is confirmed there

# Architecture Notes

- Source of issue:
  - The current evidence points to duplicated route-level JSX decorations, not a shared primitive or CSS pseudo-element.
- Fix scope:
  - Prefer component-local removal or tone-down over global CSS changes.
- Safety:
  - Keep the visual direction intact by removing only the offending yellow circle, not every ambient glow.

# File Creation / Modification / Deletion Summary

- Modify `src/components/home/home-hero.tsx`
  - Remove or tone down the amber radial circle decoration.
- Modify `src/components/pokemon-detail/pokemon-detail-hero.tsx`
  - Remove or tone down the matching amber radial circle decoration.
- Leave `src/app/globals.css` unchanged unless the implementation proves the issue is shared there.

# Execution Order

1. Confirm the issue source is duplicated route-level hero decoration.
2. Update planning artifacts for the focused UI bug.
3. Commit planning artifacts with a planning-prefixed commit.
4. Remove or neutralize the amber circle decoration in `home-hero.tsx`.
5. Remove or neutralize the matching amber circle decoration in `pokemon-detail-hero.tsx`.
6. Run verification for lint/build and refresh workflow artifacts as needed.

# Verification Expectations

- Required automated checks:
  - `pnpm lint`
  - `pnpm build`
- Required manual verification:
  - landing page no longer shows the unwanted yellow circle
  - Pokemon profile page no longer shows the unwanted yellow circle
  - blue glow and overall page atmosphere still feel intentional
- Test impact:
  - No automated tests are expected for this purely visual route-level fix.

# Open Decisions

- Fix style:
  - Option A: remove the yellow circle entirely
  - Option B: reduce its size/opacity so it becomes a subtle accent
  - Recommendation: Option A, because the user has identified it as an unwanted artifact rather than a decoration that merely needs tuning.
