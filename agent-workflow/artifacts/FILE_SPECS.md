<!-- Generated by next-codex-workflow-kit -->
# File Specifications

## `src/app/globals.css`, `src/app/layout.tsx`, `src/app/page.tsx`

- Action: Modify
- Purpose: Restore the single-agent shell, typography direction, and homepage framing.
- Responsibility: Re-establish the top-level visual identity and route composition patterns used by the single-agent branch.
- Public API: App shell and home route only.
- Inputs: `origin/single-agent` implementations as alignment targets.
- Outputs: Restored visual direction and homepage structure.
- Dependencies: `src/components/home/*`, `src/components/ui/*`.
- Rendering mode: Server-first route and global stylesheet.
- Test impact: Indirect only; validated by lint/build/manual review.
- Verification expectations: Home route should visibly match the single-agent direction rather than the current multi-agent look.

## `src/components/home/*`, `src/content/home.ts`, `src/components/ui/*`

- Action: Modify/create/delete as needed
- Purpose: Re-align shared primitives and home composition with the single-agent branch.
- Responsibility: Restore the primitive set (`Eyebrow`, `Panel`, related patterns) and the home-route composition/content structure expected by the target branch.
- Public API: Presentational components only.
- Inputs: Single-agent component APIs and content structure.
- Outputs: Home and shared UI components aligned to the target branch.
- Dependencies: Global shell and styling tokens.
- Rendering mode: Server components by default.
- Test impact: Mostly manual/browser-level.
- Verification expectations: Component structure and visual hierarchy should read like the single-agent branch.

## `src/lib/pokemon/*`, `src/lib/pokedex/*`, `next.config.ts`, `package.json`, `pnpm-lock.yaml`

- Action: Modify/create/delete as needed
- Purpose: Re-align the data/config architecture to the single-agent `pokemonService` model.
- Responsibility: Restore the service/model/raw-type/query organization from single-agent, while carrying forward current safe/tested improvements that still belong.
- Public API: Route-facing data/service layer.
- Inputs: Current data-layer improvements and single-agent architecture target.
- Outputs: Stable public API for browse/detail routes that looks like the single-agent branch.
- Dependencies: PokeAPI fetches, normalization logic, current tests.
- Rendering mode: Server-compatible utility modules and config files.
- Test impact: High; keep normalization/evolution tests green through the refactor.
- Verification expectations: `pnpm test`, `pnpm lint`, and `pnpm build` must all stay green after the architecture swap.

## `src/app/pokedex/page.tsx`, `src/app/pokedex/loading.tsx`, `src/components/pokedex/*`

- Action: Modify/create/delete as needed
- Purpose: Restore the single-agent browse-route structure and presentation.
- Responsibility: Align browse composition, query handling, and UI density to the single-agent branch without silently losing current safety fixes.
- Public API: `/pokedex` route and its presentational components.
- Inputs: Single-agent browse route/component structure and restored data service layer.
- Outputs: Server-rendered browse route aligned to target branch.
- Dependencies: `src/lib/pokemon/*`, shared UI primitives.
- Rendering mode: Server-first route with route-level loading behavior as needed.
- Test impact: Mostly browser-level later; keep existing automated checks green.
- Verification expectations: Browse route should align structurally and visually with single-agent.

## `src/app/pokedex/[pokemon]/*`, `src/components/pokemon-detail/*`

- Action: Create/modify/delete as needed
- Purpose: Replace the current `[slug]` route family with the single-agent `[pokemon]` detail-route family.
- Responsibility: Restore route naming, route files, and detail composition to the single-agent model while preserving valid safety/test improvements.
- Public API: Canonical detail route and its presentational components.
- Inputs: Single-agent detail route/component structure plus any current fixes worth keeping.
- Outputs: Detail route aligned to target branch.
- Dependencies: Restored `pokemonService` layer, shared UI primitives.
- Rendering mode: Server-first route with route-level loading/not-found/error behavior if preserved.
- Test impact: Keep normalization tests aligned with whatever data contract survives the refactor.
- Verification expectations: Detail route should match single-agent route shape and presentation, with no raw error leakage.
