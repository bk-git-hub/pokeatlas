<!-- Generated by next-codex-workflow-kit -->
# Review Scope

- Focused hero-decoration bug fix
- Files reviewed: `src/components/home/home-hero.tsx`, `src/components/pokemon-detail/pokemon-detail-hero.tsx`
- Baseline used: `change-review`, `next-best-practices`, `vercel-react-best-practices`

# Findings

- No blocking or medium-severity findings in this fix.

# Plan Compliance

- The implementation stays within the approved scope from `05d1c8f`: remove the duplicated yellow hero decoration from the landing and Pokemon detail heroes only.
- The fix is route-local. It does not change `globals.css`, shared primitives, or the broader visual system.
- The blue glow and the rest of the hero structure remain intact, which matches the planned minimal-fix strategy.

# Next.js / React Quality

- [home-hero.tsx](C:/Users/bksoft/Desktop/pokeatlas/src/components/home/home-hero.tsx#L1) now removes only the explicit amber radial node instead of rewriting the hero layout.
- [pokemon-detail-hero.tsx](C:/Users/bksoft/Desktop/pokeatlas/src/components/pokemon-detail/pokemon-detail-hero.tsx#L1) applies the same local removal, which keeps the fix consistent across both affected routes.
- The change is purely presentational and does not introduce new state, effects, or route behavior.

# Regression Risks

- The main remaining risk is visual balance, not functionality: removing the yellow circle could leave the hero compositions feeling flatter if the blue glow alone is not enough.
- Because the fix is duplicated in two route components rather than centralized, similar decoration drift could reappear later if hero overlays continue to evolve independently.

# Test Coverage Gaps

- No automated tests were added. This is appropriate for the change, because there is no meaningful unit-test seam for deleting two decorative nodes.
- There is no visual regression coverage for these hero decorations.
- The browser QA gap remains: `/` and `/pokedex/[pokemon]` still need direct visual confirmation after the fix.

# Residual Risks / Follow-up

- Verify in a real browser that the yellow circle is gone on the landing page and Pokemon profile page.
- Confirm the pages still retain enough atmosphere after the amber circle removal.
- If similar issues recur, consider whether hero-decoration patterns should be centralized rather than duplicated per route.
