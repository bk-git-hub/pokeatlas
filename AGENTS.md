<!-- Generated by next-codex-workflow-kit -->
# Repository Overview

Detected router style: App Router

Important source roots:
- `src/app/`

Workflow artifacts live in `agent-workflow/artifacts/`.

Current workflow mode: multi-agent

# Commands

- Dev: `pnpm dev`
- Build: `pnpm build`
- Lint: `pnpm lint`
- Performance audit: `node scripts/run-lighthouse.mjs`

# Workflow Shortcuts

- `$plan-feature` to route work through `explorer` then `planner` before coding.
- `$build-feature` to route approved implementation or refactor work through `executor`, `tester`, then verification and review.
- `$verify-feature` to run verification through `verifier` and update `VERIFY.md`.
- `$review-feature` to run final review through `reviewer` and update `REVIEW.md`.

# Workflow Artifacts

- `agent-workflow/artifacts/PLAN.md`
- `agent-workflow/artifacts/FILE_SPECS.md`
- `agent-workflow/artifacts/DECISION.md`
- `agent-workflow/artifacts/VERIFY.md`
- `agent-workflow/artifacts/REVIEW.md`
- `agent-workflow/artifacts/PERF.md`

# Feature Work Routing Rules

1. For non-trivial feature work, do not write implementation code before `PLAN.md` and `FILE_SPECS.md` exist.
2. Use the planning workflow before introducing new routes, server/client boundaries, state ownership changes, or major UI composition changes.
3. If more than one viable strategy exists, ask the user to choose.
4. After a user decision, update `DECISION.md`.
5. Route non-trivial planning through `$plan-feature` so `explorer` surfaces the current repo patterns before `planner` writes the artifacts.
6. Route non-trivial implementation or refactor work through `$build-feature` so `executor`, `tester`, `verifier`, `reviewer`, and the repo-local quality skills are all used.
7. After implementation, run verification and update `VERIFY.md`.
8. After verification, run structured review and update `REVIEW.md`.
9. If performance mode is enabled and user-facing routes changed, run performance audit and update `PERF.md`.
10. Final summaries must reference workflow artifact outputs.

# Definition of Done

- Implementation completed.
- Tests updated or deliberately left unchanged with rationale.
- Verification attempted.
- Review completed.
- Decisions logged when required.
- Remaining risks called out explicitly.
